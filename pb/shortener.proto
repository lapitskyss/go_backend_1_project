syntax = "proto3";

package shortener;

// -----------------Link service-----------------

service LinkService {
    rpc GetLink(GetLinkRequest) returns (Link) {}
}

message GetLinkRequest {
    string hash = 1;
    string user_agent = 2;
}

message Link {
    string url = 1;
    string hash = 2;
}
