version: '2.4'

services:
    linkservice:
        build:
            context: ./src/linkservice/
            dockerfile: Dockerfile
            target: develop
        container_name: ${APP_NAME}_linkservice
        ports:
            - "${LINKSERVICE_HTTP_PORT}:${LINKSERVICE_HTTP_PORT}"
        environment:
            POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
            POSTGRES_DB: ${POSTGRES_DB}
            POSTGRES_USER: ${POSTGRES_USER}
            POSTGRES_PORT: ${POSTGRES_PORT}
            LINKSERVICE_HTTP_PORT: ${LINKSERVICE_HTTP_PORT}
            LINKSERVICE_GRPC_PORT: ${LINKSERVICE_GRPC_PORT}
        depends_on:
            - postgres
            - flyway
        volumes:
            - ./src/linkservice:/app

    frontend:
        build:
            context: ./src/frontend/
            dockerfile: Dockerfile
            target: develop
        container_name: ${APP_NAME}_frontend
        ports:
            - "${FRONTEND_HTTP_PORT}:${FRONTEND_HTTP_PORT}"
        environment:
            POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
            POSTGRES_DB: ${POSTGRES_DB}
            POSTGRES_USER: ${POSTGRES_USER}
            POSTGRES_PORT: ${POSTGRES_PORT}
            FRONTEND_HTTP_PORT: ${FRONTEND_HTTP_PORT}
            LINKSERVICE_SERVICE_ADDR: ${LINKSERVICE_SERVICE_ADDR}
        volumes:
            - ./src/frontend:/app

    postgres:
        image: postgres:12.6
        container_name: ${APP_NAME}_postges
        ports:
            - "${POSTGRES_PORT}:5432"
        environment:
            POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
            POSTGRES_DB: ${POSTGRES_DB}
            POSTGRES_USER: ${POSTGRES_USER}
            POSTGRES_PORT: ${POSTGRES_PORT}
        volumes:
            - postgres:/data/postgres

    flyway:
        image: flyway/flyway
        container_name: ${APP_NAME}_flyway
        command: -url=jdbc:postgresql://postgres:${POSTGRES_PORT}/${POSTGRES_DB} -user=${POSTGRES_USER} -password=${POSTGRES_PASSWORD} -connectRetries=60 migrate
        volumes:
            - ./db/migration:/flyway/sql
        depends_on:
            - postgres

    pgadmin:
        image: dpage/pgadmin4:5
        container_name: ${APP_NAME}_pgadmin
        ports:
            - "${PGADMIN_PORT}:80"
        environment:
            PGADMIN_DEFAULT_EMAIL: ${PGADMIN_EMAIL}
            PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_PASSWORD}
            PGADMIN_CONFIG_SERVER_MODE: 'False'
        volumes:
            - pgadmin:/root/.pgadmin
        depends_on:
            - postgres

volumes:
    postgres:
    pgadmin:
